// Tipo de dados primitivos => String, number, underfined, null, boolean, symbol

const nome = 'Elias'; //string
const nome1 = "Elias"; //string
const nome2 = `Elias`; //string
const num1 = 10; //number
const num2 = 10.52; //number
let nomeAluno; // undefined = não aponta pra local nehum na memória
let sobrenomeAluno = null; //Nulo -> não aponta para local nenhum na memória
const aprovado = false; //Boolean = true, false (lógico)

const a = [1, 2];
const b = a;

console.log(a, b);
//console.log(typeof nome, nome); // verifica o tipo da variavel e o valor dela


